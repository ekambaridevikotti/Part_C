# Part C - Closures

This folder contains the solutions for Part C of the JavaScript Assignment.

## Q10 - Simple Closure

### Task:
Create a `counter()` function that returns another function.
Each call to the inner function should increment and return the next number.

Solution is written in **Q10.js**.

---

## Q11 - Medium Closure

### Task:
Create a `createWallet()` function that returns:
- `addMoney(amount)`
- `checkBalance()`

The balance must remain private using closure.

Solution is written in **Q11.js**.

---

### Submitted by: Ekambari Devi Kotti
